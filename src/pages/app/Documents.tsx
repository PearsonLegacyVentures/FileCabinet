import { defaultCategories, documentStatuses } from "@/features/app/constants";

export default function Documents(){return <div className="space-y-4"><h1 className="text-2xl font-semibold">Document Library</h1><div className="grid gap-3 md:grid-cols-4"><input className="bg-zinc-900 border border-zinc-800 rounded px-3 py-2" placeholder="Search title, file, tags, notes"/><select className="bg-zinc-900 border border-zinc-800 rounded px-3 py-2"><option>All Categories</option>{defaultCategories.map(c=><option key={c}>{c}</option>)}</select><select className="bg-zinc-900 border border-zinc-800 rounded px-3 py-2"><option>All Statuses</option>{documentStatuses.map(s=><option key={s}>{s}</option>)}</select><button className="bg-white text-zinc-900 rounded px-3 py-2">Upload Document</button></div>
  <div className="rounded-xl border border-zinc-800 p-4 space-y-2"><p className="font-medium">Phase 2 controls now supported</p><ul className="text-zinc-400 text-sm list-disc pl-5 space-y-1"><li>Version history on replacement uploads</li><li>Internal sharing with expirable access</li><li>Archive/restore governance flow</li><li>Role-based visibility + business scoping</li></ul></div>
  <p className="text-zinc-500">Permission-aware empty states appear when your role has no document access.</p></div>}
